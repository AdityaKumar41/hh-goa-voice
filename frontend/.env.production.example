# Add this in Vercel Project Settings > Environment Variables, then redeploy.
# Keep https://. The frontend also normalizes a bare hostname defensively.
VITE_API_URL=https://hh-goa-voice-production.up.railway.app
